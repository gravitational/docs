Here are instructions for installing version {{ version }}:

```code
$ curl -OL https://example.com/downloads/mysoftware-v{{ version }}.tar
$ tar -xvf mysoftware-v{{ version }}.tar
```

We no longer support version {{ unsupported }}.
