## Header

```yaml
version: v2
teleport:
  join_params:
    token_name: "/tmp/token"
    method: token
  auth_servers:
  - "<Var name="teleport.example.com:3080" description="URI for your Teleport Auth Server or Teleport Cloud tenant"/>"
proxy_service:
```
